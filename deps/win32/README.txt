Installers here are for all the Python (2.7) extensions necessary to run the scripts in /tools.  Additionally, you'll need to install the following libraries on the command line:

$ C:\Python27\Scripts\pip.exe install psd_tools

